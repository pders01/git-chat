# Build output
/dist/
/git-chat

# Go
*.test
*.out
/vendor/

# Web build output (embedded into Go binary at build time).
# We commit only the stub index.html so `go build` works on a fresh clone.
/internal/assets/dist/*
!/internal/assets/dist/index.html

# Web dev
/web/node_modules
/web/dist
/web/.vite

# SSH host keys (generated at first run)
host_ed25519*

# Test artifacts
/test-results/
/web/test-results/

# Claude Code worktrees
/.claude/worktrees/

# Editor / OS
.DS_Store
.idea/
.vscode/
HANDOFF.md
.claude/settings.local.json
